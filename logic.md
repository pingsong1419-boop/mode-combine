# MES 工序扫码系统 - 业务流程逻辑详细说明书

本文档旨在详尽描述模拟堆叠站扫码系统的核心业务逻辑、数据流转路径以及异常处理机制。

---

## 1. 系统架构与通讯基础

### 1.1 组件分工
- **前端 (Vue 3 + Vite)**: 
  - 承载所有的业务逻辑状态机（State Machine）。
  - 处理条码正则匹配、异步 API 调用调度以及数据清洗（Cleaning）逻辑。
  - **非持久化原则**: 扫描框数据与系统状态不进行本地缓存（LocalStorage），刷新页面即重置，以确保操作环境的纯净。
- **后端 (.NET SignalR Hub)**:
  - 实时数据中继站，负责监听 PLC 寄存器变化（通过 S7 协议）。
  - 当读取到堆叠完成信号（Pulse）时，批量采集电芯条码并推送到前端。
- **外部 API (MES 系统)**:
  - 提供工单（Order）、工艺路线（Route/BOM）、单物料校验（Single Check）及重码校验（Duplicate Check）接口。

---

## 2. 触发与工单拉取逻辑 (核心流程)

系统严格执行以下 5 个步骤来确保工单匹配的准确性：

### 2.1 触发机制 (Dual-Trigger)
- **方式 A：人工扫码**: 操作员直接在扫描框内输入产品 SN，按下回车。
- **方式 B：PLC 后台触发**: 
  - 系统接收到 PLC 堆叠完成信号，自动采集电芯矩阵。
  - **静默处理**: 系统自动取采集到的第一个电芯条码作为“临时 SN”触发查询。
  - **UI 保护**: 此过程**不会**修改界面上“扫描产品条码”输入框的内容，保持人工输入的独立性。

### 2.2 筛选与 SN 定义流程
- **步骤 1：状态初步筛选**
  - 调用 `getOrderByProcess` 接口，从 MES 获取当前产线的所有工单。
  - **规则**: 仅保留 `order_Status` 等于 **"2"** (待生产) 的工单。
- **步骤 2：类型二次筛选**
  - 对步骤 1 的结果进一步过滤。
  - **规则**: 仅保留 `order_Type` 为 **0** (普通工单) 或 **1** (主工单) 的项。
- **步骤 3：人工选择与 SN 锁定**
  - 如果筛选结果 > 0，弹出工单选择框。
  - 操作员选中某条工单后，该工单的 `code` 被标记为当前的 **"SN"** (Session SN)。
  - 系统自动拉取对应的工艺路线 (BOM)。

### 2.3 二次触发与匹配验证 (Secondary Logic)
当系统在已有活跃 SN 的情况下再次收到触发信号（如 PLC 连打）时：
- **匹配验证**: 重新获取工单列表，检查记录的 "SN" 是否依然存在且 `order_Status` 仍为 **"2"**。
  - **通过**: 系统静默继续，不弹出窗口。如果校验已完成，自动跳转至“获取信息”页。
  - **状态改变**: 若 SN 状态变为非 2，系统判定任务失效，**强制弹出**工单选择框供人工重选。
  - **未找到 Code**: 若最新列表中无此 SN，判定为异常触发，**强制弹出**工单选择框。
- **智能重置**: 只有当输入了与当前 SN 不同的**全新条码**时，系统才会重置所有矩阵数据和校验结果。

---

## 3. 校验流水线与数据清洗

### 3.1 采集矩阵
- **结构**: 3列 x N层（层数由 PLC 动态传递）。
- **填充顺序**: 接收到 PLC 数组后，按顺序填充至前端矩阵视图。

### 3.2 三层并行校验
每一个进入矩阵的条码都会触发：
1. **规则校验 (Rule Match)**: 
   - 检查条码前缀是否匹配 BOM 物料编号。
   - 检查条码长度是否符合 `noLength` 定义。
2. **重码校验 (Duplicate Check)**: 调用 API 确保该条码未被重复使用。
3. **单物料校验 (Single Check)**: 调用 API 确认条码属于当前工单。

### 3.3 数据清洗规则 (Final Result Generation)
系统根据上述校验结果，为矩阵每个位置生成 **24位** 最终结果：
- **OK (全绿)**: 保留**原始条码**。
- **NG (任意一环失败)**: 结果强制替换为 **`24个0`** (`000000000000000000000000`)。
- **EMPTY (未采集)**: 结果强制填充为 **`24个9`** (`999999999999999999999999`)。

---

## 4. 完成判定与自动导航

### 4.1 矩阵驱动完成逻辑
- **判定标准**: 
  - 矩阵中所有格子均已填满。
  - 所有非空条码的异步校验请求（Loading）均已返回结果。
  - 无论结果是 OK 还是 NG，只要状态已确定（Processed），即判定为完成。
- **解除干扰**: 系统的完成状态不再受 BOM 中其他非电芯类冗余物料的挂起状态影响。

### 4.2 自动跳转
- 当判定为“校验完成”后：
  1. 系统自动执行 `setOK` 操作（综合判定）。
  2. 界面从“物料验证”标签页自动切换至 **“获取信息”** 标签页，向操作员展示最终的清洗结果矩阵。
