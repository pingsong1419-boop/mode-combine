# MES 工序扫码系统 - 业务流程逻辑说明 (Logic Flow)

本文档描述了模拟堆叠站扫码系统的核心业务逻辑与数据流转过程。

## 1. 系统架构概述
- **前端 (Vue 3)**: 负责界面交互、条码矩阵展示、数据清洗逻辑与状态控制。
- **后端 (.NET Hub)**: 作为中间件，通过 SignalR 实时转发 PLC 数据，并与前端通信。
- **MES 接口**: 提供工单查询、工艺路线获取、单物料校验及重码校验等 API 支持。
- **PLC 通讯**: 通过 S7 协议读取生产线实时状态与条码采集矩阵。

## 2. 核心业务流程

### 2.1 初始化与连接
- 系统启动后，前端通过 SignalR 连接到后端 `torqueHub`。
- 加载本地配置（PLC IP、API 地址、工艺代码等）。
- 监听 PLC 的心跳信号与触发信号。

### 2.2 触发与工单拉取
- **触发方式**:
  - 人工扫码产品 SN。
  - PLC 触发信号（模拟或真实地址）。
- **执行逻辑**:
  1. 调用 `getOrderByProcess` 获取当前产线的待生产工单。
  2. 匹配对应 SN 的工单信息。
  3. 调用 `getRouteList` 获取该工单的工艺路线及物料清单 (BOM)。
  4. 自动切换至“物料验证”标签页。

### 2.3 电芯条码采集矩阵
- PLC 一次性发送或分批发送条码矩阵数据（通常为 3列 x N层）。
- 前端接收到条码列表后，按列优先顺序填充“电芯采集矩阵”。

### 2.4 校验流水线 (Validation Pipeline)
对于矩阵中的每一个条码，系统会自动执行以下三层校验：
1. **规则校验 (Rule Match)**: 
   - 检查条码是否符合 BOM 中定义的“物料编号”前缀。
   - 检查条码长度是否符合要求。
2. **重码校验 (Duplicate Check)**:
   - 调用外部 API 检查该条码是否在生产记录中重复。
3. **单物料校验 (Single Material Check)**:
   - 调用外部 API 确认该条码所属物料是否属于当前工单。

### 2.5 数据清洗与结果处理
系统根据校验结果对原始条码进行“清洗”，生成 24 位最终结果：
- **通过**: 保留原始条码。
- **校验失败**: 如果任意一项校验（规则、单物料、重码）不通过，条码被替换为 `24个0`。
- **空位补充**: 如果矩阵中某个位置未采集到条码，补充为 `24个9`。

### 2.6 完成判定与跳转
- **完成条件**: 当矩阵中所有位置（3xN）均已填充条码，且每一个条码的异步校验（Loading 状态）均已结束时，系统判定“校验完成”。
- **后续动作**:
  1. 顶层状态更新为“✅ 校验完成”。
  2. 自动跳转到 **“获取信息”** 标签页，供操作员查看最终结果详情。

## 3. 关键状态转换
- `IDLE` (待机) -> `LOADING` (获取数据) -> `VALIDATING` (校验中) -> `OK/NG` (完成)
- **forceComplete 逻辑**: 为了解决 BOM 冗余物料干扰，系统优先以“矩阵处理结果”是否全部产出来判定全局流程是否结束。
