# MES 工序扫码系统 - 开发与修改日志

本文档记录了项目开发过程中的关键步骤、时间节点及具体的软件修改内容。

---

## 📅 2026-04-21

### 1. 项目环境梳理与定位 (13:38 - 13:40)
- **动作**：对工作空间目录进行全量扫描。
- **内容**：
    - 明确了目标项目为 `南京线体标准界面` (基于 Vue 3 + .NET Core 的 MES 扫码系统)。
    - **修正**：撤销了由于初始误读在 `bms-auto-linker` 项目中创建的文档和 README 修改，确保生产环境纯净。

### 2. 建立技术文档体系 (13:40 - 13:41)
- **修改文件**：
    - `docs/project_understanding.md` (新增)：详细记录了 MES 接口对接、Open Protocol 协议实现、SignalR 通信及采集马头 (Desoutter) 定扭控制器数据。
    - `docs/coding_rules.md` (新增)：制定了前后端开发规范，包括异步编程、类型安全、命名标准等。
    - `README.md` (更新)：添加了文档导航链接。

### 3. 系统运行与联调 (13:42 - 13:43)
- **动作**：启动完整服务链。
- **内容**：
    - **前端**：通过 `npm run dev` 启动 Vite 开发服务器 (Port: 5173)。
    - **后端**：通过 `dotnet run` 启动 .NET 通信后端 (Port: 5246)。
    - **连接验证**：后端成功建立与定扭控制器 (`192.168.5.212:4545`) 的 TCP 连接并开始心跳握手。
    - **浏览器端**：通过自动化工具验证了 UI 界面在 `http://localhost:5173/` 的加载情况。

### 4. 仿真功能精简：删除模拟定扭 (13:44 - 13:45)
- **背景**：基础功能精简。
- **软件修改内容**：移除 `SimulatorControl` 中的硬件数据模拟部分。

### 5. 彻底移除模拟交互控制台 (13:51 - 13:52)
- **动作**：按规则文件执行。
- **内容**：
    - **文件删除**：彻底删除了 `src/components/SimulatorControl.vue`。
    - **逻辑清理**：
        - 从 `src/App.vue` 中移除了组件导入和模板标签。
        - 确保系统不再暴露任何仿真操作入口，完全进入生产模式。
### 6. 彻底移除 LabVIEW 相关逻辑 (13:53 - 13:54)
- **背景**：按规则文件执行。
- **内容**：
    - **文件删除**：删除了 `src/utils/labviewSignal.ts` 及其所有 HTTP 请求代码。
    - **UI/逻辑清理**：
        - 移除了 `App.vue` 中的 `writeSignal`/`clearSignal` 调用。
        - 隐藏了界面标题中的 “→ LabVIEW 信号” 指示。
    - **结果**：系统现在专注于扫码验证与定扭数据采集，不再向外部 LabVIEW 系统输出信号。

---

## 🛠 当前系统状态
- **核心逻辑**：完全由 SignalR 实时推送真实的硬件测量值，接口返回统一的 JSON 格式。
- **文档状态**：已建立完整的理解文档和开发规范。
- **服务状态**：前后端联调正常运行，服务已准备好进行下一步部署。

---

### 🏁 2026-04-21 项目阶段性关闭 (18:32)
- **总结**：
    - 完成了 PLC 通讯模块的集成与注册。
    - 移除了冗余的仿真 (Simulator) 与 LabVIEW 逻辑。
    - 完善了技术文档与开发规范。
    - 系统已进入生产就绪状态，专注于 MES 扫码与定扭数据采集核心功能。
- **后记**：项目文件已全部保存，环境已清理。
